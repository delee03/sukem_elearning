import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: ['readymadeui.com'], // Add the hostname here
  },
  /* config options here */
  // Test account github
};

export default nextConfig;
